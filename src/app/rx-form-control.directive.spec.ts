import { RxFormControlDirective } from './rx-form-control.directive';

describe('RxFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new RxFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
