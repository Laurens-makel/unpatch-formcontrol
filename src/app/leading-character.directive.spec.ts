import { LeadingCharacterDirective } from './leading-character.directive';

describe('LeadingCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new LeadingCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
